import react from 'react';
import reactDom from 'react-dom/client';

import App from './App';
const root= reactDom.createRoot(document.getElementById('root'));// creates a id with root in index.html file
root.render(<App/>);


